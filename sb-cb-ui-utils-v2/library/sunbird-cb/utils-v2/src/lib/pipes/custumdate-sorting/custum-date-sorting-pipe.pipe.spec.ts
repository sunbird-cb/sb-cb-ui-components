import { CustumDateSortingPipePipe } from './custum-date-sorting-pipe.pipe'

describe('CustumDateSortingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustumDateSortingPipePipe()
    expect(pipe).toBeTruthy()
  })
})
